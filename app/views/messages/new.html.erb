<%= form_for [@user, @message] do |f| %>
  <%= f.label :recipient_id %>
  <%= f.select :recipient_id, @hosts.map{|u| [u.email, u.id]} %>
  
  <%= f.label :body %>
  <%= f.text_field :body %>
<% end %>
